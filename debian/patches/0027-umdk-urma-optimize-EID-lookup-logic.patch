From 4921013633ca27cc864cc6ca30e75668177bfa68 Mon Sep 17 00:00:00 2001
From: wanghang73 <wanghang73@huawei.com>
Date: Fri, 27 Feb 2026 12:24:31 +0800
Subject: [PATCH] 0027

Signed-off-by: wanghang73 <wanghang73@huawei.com>
---
 src/urma/lib/urma/core/CMakeLists.txt      |  16 ++++
 src/urma/tools/urma_admin/CMakeLists.txt   |   2 +-
 src/urma/tools/urma_admin/admin_cmd.h      |   4 +-
 src/urma/tools/urma_admin/admin_cmd_agg.c  |  87 +++++++++---------
 src/urma/tools/urma_admin/admin_cmd_show.c | 102 ++++++++++++++-------
 5 files changed, 130 insertions(+), 81 deletions(-)

diff --git a/src/urma/lib/urma/core/CMakeLists.txt b/src/urma/lib/urma/core/CMakeLists.txt
index 45e1a8e..a5eb782 100644
--- a/src/urma/lib/urma/core/CMakeLists.txt
+++ b/src/urma/lib/urma/core/CMakeLists.txt
@@ -7,20 +7,36 @@ add_library(urma SHARED
     ${URMA_DIR_SRCS}
 )
 
+add_library(urma_static STATIC
+    ${URMA_DIR_SRCS}
+)
+
 set_target_properties(urma
     PROPERTIES
         C_STANDARD 99
         VERSION ${PROJECT_VERSION}
         SOVERSION ${PROJECT_VERSION_MAJOR}
 )
+set_target_properties(urma_static
+    PROPERTIES
+        C_STANDARD 99
+        VERSION ${PROJECT_VERSION}
+        SOVERSION ${PROJECT_VERSION_MAJOR}
+)
 
 target_include_directories(urma
     PUBLIC
         ${CMAKE_CURRENT_SOURCE_DIR}/include
         ${CMAKE_CURRENT_SOURCE_DIR}
 )
+target_include_directories(urma_static
+    PUBLIC
+        ${CMAKE_CURRENT_SOURCE_DIR}/include
+        ${CMAKE_CURRENT_SOURCE_DIR}
+)
 
 target_link_libraries(urma urma_common -ldl -lrt)
+target_link_libraries(urma_static urma_common -ldl -lrt)
 
 install(TARGETS urma
     DESTINATION /usr/lib64
diff --git a/src/urma/tools/urma_admin/CMakeLists.txt b/src/urma/tools/urma_admin/CMakeLists.txt
index 1660163..730018e 100644
--- a/src/urma/tools/urma_admin/CMakeLists.txt
+++ b/src/urma/tools/urma_admin/CMakeLists.txt
@@ -31,7 +31,7 @@ target_include_directories(urma_admin
 
 target_link_libraries(urma_admin
     PRIVATE
-        nl-genl-3 nl-3
+        nl-genl-3 nl-3 urma_static
 )
 
 install(TARGETS urma_admin
diff --git a/src/urma/tools/urma_admin/admin_cmd.h b/src/urma/tools/urma_admin/admin_cmd.h
index d0e7ba6..7e6a8a7 100644
--- a/src/urma/tools/urma_admin/admin_cmd.h
+++ b/src/urma/tools/urma_admin/admin_cmd.h
@@ -189,8 +189,8 @@ int admin_nl_unexpose_dev_ns(const char *dev_name, int ns_fd);
 int admin_nl_set_eid_ns(const char *dev_name, uint32_t eid_idx, int ns_fd);
 int admin_cmd_get_topo_info(tool_topo_map_t *topo_map);
 int admin_get_device_name_by_eid(const urma_eid_t *eid, char *dev_name, size_t dev_name_len);
-int admin_get_device_info_by_eid(const urma_eid_t *eid, admin_device_info_t *device_info, 
-                                 struct ub_list *ubep_list);
+int admin_get_eid_list_by_eid(urma_eid_t *eid, urma_eid_info_t **eid_info_list,
+                              char* dev_name);
 
 // Legacy command
 int admin_cmd_show_stats_legacy(admin_config_t *cfg);
diff --git a/src/urma/tools/urma_admin/admin_cmd_agg.c b/src/urma/tools/urma_admin/admin_cmd_agg.c
index f6b235b..8591cec 100644
--- a/src/urma/tools/urma_admin/admin_cmd_agg.c
+++ b/src/urma/tools/urma_admin/admin_cmd_agg.c
@@ -134,15 +134,15 @@ static tool_topo_agg_dev_t *get_topo_agg_dev_by_agg_eid(tool_topo_map_t *topo_ma
     return NULL;
 }
 
-static int get_eid_idx(admin_device_info_t *dev_info, urma_eid_t *input_eid)
+static int get_eid_idx_from_list(urma_eid_info_t *eid_list, urma_eid_t *target_eid)
 {
-    if (urma_eid_is_valid(input_eid) == false) {
+    if (urma_eid_is_valid(target_eid) == false) {
         return -1;
     }
 
-    for (uint32_t i = 0; i < dev_info->dev_attr.dev_cap.max_eid_cnt; i++) {
-        if (memcmp(&dev_info->eid_list[i].eid, input_eid, sizeof(urma_eid_t)) == 0) {
-            return (int)i;
+    for (uint32_t i = 0; i < URMA_MAX_EID_CNT; i++) {
+        if (memcmp(&eid_list[i].eid, target_eid, sizeof(urma_eid_t)) == 0) {
+            return eid_list[i].eid_index;
         }
     }
 
@@ -166,99 +166,100 @@ static void nl_unexpose_device_by_eid(const char *eid, int ns_fd)
 
 static int nl_expose_agg_device(struct tool_topo_agg_dev *agg_dev, int ns_fd)
 {
-    struct ub_list ubep_list;
     int ret = 0;
     int ue_idx = 0;
-    admin_device_info_t bonding_dev_info = {0};
+    urma_eid_info_t *bonding_eid_list = NULL;
+    char bonding_dev_name[URMA_MAX_NAME] = {0};
 
-    ub_list_init(&ubep_list);
-    ret = admin_get_device_info_by_eid((urma_eid_t *)agg_dev->agg_eid, &bonding_dev_info, &ubep_list);
+    (void)printf("bonding EID: " EID_FMT "\n", EID_RAW_ARGS(agg_dev->agg_eid));
+
+    ret = admin_get_eid_list_by_eid((urma_eid_t *)agg_dev->agg_eid, &bonding_eid_list, bonding_dev_name);
     if (ret != 0) {
         printf("Failed to get device info for bonding device, ret=%d\n", ret);
         return ret;
     }
 
-    int bonding_eid_idx = get_eid_idx(&bonding_dev_info, (urma_eid_t *)agg_dev->agg_eid);
+    int bonding_eid_idx = get_eid_idx_from_list(bonding_eid_list, (urma_eid_t *)agg_dev->agg_eid);
     if (bonding_eid_idx < 0) {
         printf("Failed to get EID index for bonding device\n");
-        free(bonding_dev_info.eid_list);
+        urma_free_eid_list(bonding_eid_list);
         return -EINVAL;
     }
 
-    printf("Exposing bonding device %s in netns %d\n", bonding_dev_info.dev_name, ns_fd);
-    ret = admin_nl_expose_dev_ns(bonding_dev_info.dev_name, ns_fd);
+    printf("Exposing bonding device %s in netns %d\n", bonding_dev_name, ns_fd);
+    ret = admin_nl_expose_dev_ns(bonding_dev_name, ns_fd);
     if (ret != 0) {
-        printf("Failed to expose bonding device %s in netns %d, ret=%d\n", bonding_dev_info.dev_name, ns_fd, ret);
-        free(bonding_dev_info.eid_list);
+        printf("Failed to expose bonding device %s in netns %d, ret=%d\n", bonding_dev_name, ns_fd, ret);
+        urma_free_eid_list(bonding_eid_list);
         return ret;
     }
 
-    printf("Setting EID index %d in bonding device %s, netns %d\n", bonding_eid_idx, bonding_dev_info.dev_name, ns_fd);
-    ret = admin_nl_set_eid_ns(bonding_dev_info.dev_name, bonding_eid_idx, ns_fd);
+    printf("Setting EID index %d in bonding device %s, netns %d\n", bonding_eid_idx, bonding_dev_name, ns_fd);
+    ret = admin_nl_set_eid_ns(bonding_dev_name, bonding_eid_idx, ns_fd);
     if (ret != 0) {
         printf("Failed to set EID index %d in bonding device %s, netns %d, ret=%d\n", bonding_eid_idx,
-               bonding_dev_info.dev_name, ns_fd, ret);
-        admin_nl_unexpose_dev_ns(bonding_dev_info.dev_name, ns_fd);
-        free(bonding_dev_info.eid_list);
+               bonding_dev_name, ns_fd, ret);
+        admin_nl_unexpose_dev_ns(bonding_dev_name, ns_fd);
+        urma_free_eid_list(bonding_eid_list);
         return ret;
     }
 
     for (ue_idx = 0; ue_idx < IODIE_NUM; ue_idx++) {
         tool_topo_ue_t *ue = &agg_dev->ues[ue_idx];
-        admin_device_info_t primary_dev_info = {0};
-
-        ret = admin_get_device_info_by_eid((urma_eid_t *)ue->primary_eid, &primary_dev_info, &ubep_list);
+        urma_eid_info_t *primary_eid_list = NULL;
+        char primary_dev_name[URMA_MAX_NAME] = {0};
+        ret = admin_get_eid_list_by_eid((urma_eid_t *)ue->primary_eid, &primary_eid_list, primary_dev_name);
         if (ret != 0) {
             printf("Failed to get device info for primary device, ret=%d\n", ret);
             goto unexpose_agg_dev;
         }
 
-        int primary_eid_idx = get_eid_idx(&primary_dev_info, (urma_eid_t *)ue->primary_eid);
+        int primary_eid_idx = get_eid_idx_from_list(primary_eid_list, (urma_eid_t *)ue->primary_eid);
         if (primary_eid_idx < 0) {
             printf("Failed to get EID index for primary device\n");
-            free(primary_dev_info.eid_list);
+            urma_free_eid_list(primary_eid_list);
             goto unexpose_agg_dev;
         }
 
-        printf("Exposing primary device %s in netns %d\n", primary_dev_info.dev_name, ns_fd);
-        ret = admin_nl_expose_dev_ns(primary_dev_info.dev_name, ns_fd);
+        printf("Exposing primary device %s in netns %d\n", primary_dev_name, ns_fd);
+        ret = admin_nl_expose_dev_ns(primary_dev_name, ns_fd);
         if (ret != 0) {
-            printf("Failed to expose primary device %s in netns %d, ret=%d\n", primary_dev_info.dev_name, ns_fd, ret);
-            free(primary_dev_info.eid_list);
+            printf("Failed to expose primary device %s in netns %d, ret=%d\n", primary_dev_name, ns_fd, ret);
+            urma_free_eid_list(primary_eid_list);
             goto unexpose_agg_dev;
         }
 
-        printf("Setting EID index %d in primary device %s, netns %d\n", primary_eid_idx, primary_dev_info.dev_name,
+        printf("Setting EID index %d in primary device %s, netns %d\n", primary_eid_idx, primary_dev_name,
                ns_fd);
-        ret = admin_nl_set_eid_ns(primary_dev_info.dev_name, primary_eid_idx, ns_fd);
+        ret = admin_nl_set_eid_ns(primary_dev_name, primary_eid_idx, ns_fd);
         if (ret != 0) {
             printf("Failed to set EID index %d in primary device %s, netns %d, ret=%d\n", primary_eid_idx,
-                   primary_dev_info.dev_name, ns_fd, ret);
-            admin_nl_unexpose_dev_ns(primary_dev_info.dev_name, ns_fd);
-            free(primary_dev_info.eid_list);
+                   primary_dev_name, ns_fd, ret);
+            admin_nl_unexpose_dev_ns(primary_dev_name, ns_fd);
+            urma_free_eid_list(primary_eid_list);
             goto unexpose_agg_dev;
         }
 
         for (int i = 0; i < PORT_NUM; i++) {
-            int port_eid_idx = get_eid_idx(&primary_dev_info, (urma_eid_t *)ue->port_eid[i]);
+            int port_eid_idx = get_eid_idx_from_list(primary_eid_list, (urma_eid_t *)ue->port_eid[i]);
             if (port_eid_idx < 0) {
-                printf("Failed to get EID index for port EID\n");
                 continue;
             }
 
             printf("Setting port EID index %d in primary device %s, netns %d\n", port_eid_idx,
-                   primary_dev_info.dev_name, ns_fd);
-            ret = admin_nl_set_eid_ns(primary_dev_info.dev_name, port_eid_idx, ns_fd);
+                   primary_dev_name, ns_fd);
+            ret = admin_nl_set_eid_ns(primary_dev_name, port_eid_idx, ns_fd);
             if (ret != 0) {
                 printf("Failed to set port EID index %d in primary device %s, netns %d, ret=%d\n", port_eid_idx,
-                       primary_dev_info.dev_name, ns_fd, ret);
+                       primary_dev_name, ns_fd, ret);
             }
         }
 
-        free(primary_dev_info.eid_list);
+        urma_free_eid_list(primary_eid_list);
     }
 
-    free(bonding_dev_info.eid_list);
+    urma_free_eid_list(bonding_eid_list);
+
     return 0;
 
 unexpose_agg_dev:
@@ -269,8 +270,8 @@ unexpose_agg_dev:
         }
     }
 
-    admin_nl_unexpose_dev_ns(bonding_dev_info.dev_name, ns_fd);
-    free(bonding_dev_info.eid_list);
+    admin_nl_unexpose_dev_ns(bonding_dev_name, ns_fd);
+    urma_free_eid_list(bonding_eid_list);
 
     return ret;
 }
diff --git a/src/urma/tools/urma_admin/admin_cmd_show.c b/src/urma/tools/urma_admin/admin_cmd_show.c
index 5338474..ca0f3a0 100644
--- a/src/urma/tools/urma_admin/admin_cmd_show.c
+++ b/src/urma/tools/urma_admin/admin_cmd_show.c
@@ -15,6 +15,7 @@
 #include "ub_list.h"
 #include "urma_types.h"
 #include "urma_types_str.h"
+#include "urma_private.h"
 
 #include "admin_file_ops.h"
 #include "admin_log.h"
@@ -578,52 +579,83 @@ free_list:
     return ret;
 }
 
-int admin_get_device_info_by_eid(const urma_eid_t *eid, admin_device_info_t *device_info, 
-                                 struct ub_list *ubep_list)
+int admin_get_eid_list_by_eid(urma_eid_t *eid, urma_eid_info_t **eid_info_list, char* dev_name)
 {
     int ret = 0;
-    admin_show_ubep_t *ubep, *next;
-    admin_config_t cfg = {0};
-    cfg.specify_device = false;
-    admin_show_ubep_t *ubep_info = NULL;
-
-    if (ub_list_is_empty(ubep_list)) {
-        ret = find_ubep_list(ubep_list, &cfg);
+    int dev_fd = -1;
+    DIR *dir = NULL;
+    struct dirent *entry = NULL;
+    char dev_path[URMA_MAX_PATH] = {0};
+    urma_eid_info_t *tmp_eid_list = NULL;
+    uint32_t eid_cnt = 0;
+    bool found = false;
+
+    if (eid == NULL || dev_name == NULL || eid_info_list == NULL) {
+        printf("Invalid input parameters.\n");
+        return -1;
+    }
+
+    dir = opendir("/dev/uburma");
+    if (dir == NULL) {
+        printf("Failed to open /dev/uburma directory.\n");
+        return -1;
+    }
+
+    tmp_eid_list = (urma_eid_info_t *)calloc(1, URMA_MAX_EID_CNT * sizeof(urma_eid_info_t));
+    if (tmp_eid_list == NULL) {
+        printf("Failed to allocate memory for EID list.\n");
+        closedir(dir);
+        return -1;
+    }
+
+    while ((entry = readdir(dir)) != NULL) {
+        if (strcmp(entry->d_name, ".") == 0 || strcmp(entry->d_name, "..") == 0) {
+            continue;
+        }
+
+        if (snprintf(dev_path, URMA_MAX_PATH, "%s/%s", "/dev/uburma", entry->d_name) <= 0) {
+            printf("Failed to construct device path.\n");
+            continue;
+        }
+
+        dev_fd = urma_open_cdev(dev_path);
+        if (dev_fd < 0) {
+            printf("Failed to open device %s\n", entry->d_name);
+            continue;
+        }
+
+        ret = urma_cmd_get_eid_list(dev_fd, URMA_MAX_EID_CNT, tmp_eid_list, &eid_cnt);
         if (ret != 0) {
-            (void)printf("Failed to find ubep.\n");
-            return ret;
+            printf("Device %s has no EID configured\n", entry->d_name);
+            close(dev_fd);
+            continue;
         }
-    }
-
-    UB_LIST_FOR_EACH_SAFE(ubep, next, node, ubep_list) {
-        if (ubep == NULL) {
-            break;
-        }
-        for (uint32_t i = 0; i < ubep->dev_attr.dev_cap.max_eid_cnt; i++) {
-            if (is_eid_equal(&ubep->eid_list[i].eid, eid)) {
-                ubep_info = ubep;
+
+        for (uint32_t i = 0; i < eid_cnt; i++) {
+            if (memcmp(&tmp_eid_list[i].eid, eid, sizeof(urma_eid_t)) == 0) {
+                found = true;
+                strncpy(dev_name, entry->d_name, URMA_MAX_NAME - 1);
+                dev_name[URMA_MAX_NAME - 1] = '\0';
+                *eid_info_list = tmp_eid_list;
                 break;
             }
         }
-    }
-    ret = (ubep_info != NULL) ? 0 : -1;
-    if (ret == 0) {
-        memcpy(device_info->dev_name, ubep_info->dev_name, sizeof(device_info->dev_name));
-        memcpy(device_info->net_dev_name, ubep_info->net_dev_name, sizeof(device_info->net_dev_name));
-        memcpy(&device_info->dev_attr, &ubep_info->dev_attr, sizeof(urma_device_attr_t));
-        memcpy(&device_info->tp_type, &ubep_info->tp_type, sizeof(urma_transport_type_t));
 
-        device_info->eid_list = calloc(1, ubep_info->dev_attr.dev_cap.max_eid_cnt * sizeof(urma_eid_info_t));
-        if (device_info->eid_list == NULL) {
-            (void)printf("Failed to malloc eid_list.\n");
-            ret = -1;
-        } else {
-            memcpy(device_info->eid_list, ubep_info->eid_list,
-                   ubep_info->dev_attr.dev_cap.max_eid_cnt * sizeof(urma_eid_info_t));
+        close(dev_fd);
+
+        if (found) {
+            break;
         }
     }
+    closedir(dir);
 
-    return ret;
+    if (!found) {
+        printf("EID not found.\n");
+        urma_free_eid_list(tmp_eid_list);
+        return -1;
+    }
+
+    return 0;
 }
 
 bool admin_is_eid_valid(const char *eid)
-- 
2.28.0.windows.1

